edition: 1.0.0
name: fc-embedding-solution
access: default
services:
  framework:
    component: fc
    props:
      region: cn-beijing
      service:
        name: FC-Embedding
      function:
        name: towhee-server
        description: Initialize
        runtime: python3.9
        memorySize: 128
        cpu: 0.05
        timeout: 60
        codeUri: ./
        diskSize: 512
